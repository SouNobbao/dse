[dse]
; Automatically toggle DSE. If DSE was already disabled, kvc will not run.
toggleDse=true

; Appends --safe to all kvc commands
dseSafeMode=true

; Enable/disable Steam API hooks
steamHooks=false

; Enable/disable logging output
logging=false
